# Bayline Metro workstream status files

One file per workstream (`data.md`, `world.md`, `trains.md`, `infra.md`, `stations.md`, `sim.md`), owned by that workstream. See `../bart-plan.md`.
Screenshots worth keeping go in `shots/<workstream>/` as small JPEGs.
